.git
.github
.vscode
.DS_Store
**/.DS_Store
node_modules
frontend/node_modules
frontend/.next
frontend/
local/
# docs/
deploy/
# Keep initdata for workers that need seed CSVs
# initdata/
temporal-certs/
test/
testdata/
scraps/
bin/
coverage*
*.lcov
*.log
test-output
.idea
.fleet
# vendor/ is deliberately NOT ignored: the image builds resolve dependencies
# from the committed vendor tree (go build -mod=vendor) instead of the module
# proxy, so a proxy outage cannot fail a build. Excluding it again would
# silently break every Dockerfile.
coverage.html
agent-coverage.html

# Local compiled binaries in root
/askg
/epp
/escrowImport
/rest.test
/ry-admin
/unified
/mcp

# Large local data directories
backups/
grafana/
prometheus/

# Local coverage and temp db/json files
/coverage.out
/api-coverage.out
/jisc_analysis.db
/analysis_debug.json
/test_input.json
